name: Build dream.ko for Xiaomi 13 (5.15)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout driver code
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y git-core flex bison gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu libssl-dev bc

      - name: Clone kernel source (5.15 Android 13)
        run: |
          git clone --depth=1 --branch android13-5.15-lts https://android.googlesource.com/kernel/common kernel_source

      - name: Copy driver into kernel tree
        run: |
          mkdir -p kernel_source/drivers/kerneldriver
          cp -r code/* kernel_source/drivers/kerneldriver/
          cd kernel_source
          echo 'obj-y += kerneldriver/' >> drivers/Makefile

      - name: Configure kernel & force SUBLEVEL to 74
        run: |
          cd kernel_source
          sed -i 's/^SUBLEVEL = .*/SUBLEVEL = 74/' Makefile
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- gki_defconfig
          scripts/config --disable CONFIG_LOCALVERSION_AUTO
          scripts/config --set-str CONFIG_LOCALVERSION "-android13-8-00049-g8945ccdb2203-ab9500309"
          echo "0" > .scmversion
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- modules_prepare -j$(nproc)

      - name: Build dream.ko (force correct vermagic)
        run: |
          cd kernel_source
          echo "5.15.74-android13-8-00049-g8945ccdb2203-ab9500309" > include/config/kernel.release
          KBUILD_MODPOST_WARN=1 make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- M=drivers/kerneldriver modules

      - name: Upload dream.ko
        uses: actions/upload-artifact@v4
        with:
          name: dream.ko
          path: kernel_source/drivers/kerneldriver/dream.ko
